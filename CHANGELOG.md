# Changelog

## 1.5.0

Firebase / NoctuList roster, collapsible setup, hidden join QR, photo X-axis fix, manual reload, automated multi-platform GitHub Releases.

**Publishing:** commit `version.json` + `package.json` to `main`, tag `1.5.0`, then let `.github/workflows/release.yml` attach the three installers. Do not raise `minRequiredVersion` unless this update must be forced.

## 0.0.5

3D card flip animation.
